# Omnio — تحویل به سشن جدید

**تاریخ:** ۲۶ آگوست ۲۰۲۶
**ریپو:** github.com/alireza-selldone/official-omnio-marketplace · شاخه `main` · کامیت `2fb50c0`
**لوکال:** `cd "D:/omnio" && npm run dev` → http://localhost:8788
**دامنه‌ی هدف:** omnio.selldone.shop (هنوز دیپلوی نشده)

---

## ⚠️ اولین کاری که باید انجام بشه

**پروژه به فروشگاه اشتباه وصله.**

| | الان | باید |
|---|---|---|
| شناسه | `15596` | **`15631`** |
| هندل | `marko-5vMFPy7t` | ؟ (از MCP بخوان) |

فروشگاه ۱۵۵۹۶ مال پروژه‌ی دیگری است (Marko). فروشگاه واقعی Omnio شماره ۱۵۶۳۱ است و ۳۶۲ محصول با عکس‌های PNG شفاف دارد (محصولات ۷۱۳۰۸۳–۷۱۳۳۹۵).

### مراحل

۱. تأیید کن MCP به ۱۵۶۳۱ وصل است: `selldone_current_connection`
۲. هندل را از همان خروجی بردار
۳. `npm run setup -- --shop-id 15631 --handle <handle> --name Omnio`
   این اسکریپت دسته‌ها و محصولات را از XAPI می‌خواند، اسلاگ می‌سازد، `shop.config.json` را می‌نویسد و متاتگ‌ها را در HTML پخش می‌کند. اجرای دوباره‌اش امن است.
۴. اگر setup همه‌چیز را پوشش نداد، این‌ها را دستی چک کن:
   - `shop.config.json` → `shop.id`, `shop.handle`, `shop.domain`
   - `storefront/*.html` → متاتگ‌های `pajulina-shop-id`, `pajulina-shop-domain`, `pajulina-storefront-shop-handle`
   - `storefront/marketplace-config.js` → شناسه‌های وندور و `categoryIds` (مال ۱۵۵۹۶ است، باید بازسازی شود)

---

## کاری که بعد از اتصال منتظر است

**برداشتن پس‌زمینه‌ی سفید پشت عکس محصولات.**

روی ۱۵۵۹۶ فقط ۱۴۳ عکس از ۳۶۲ شفاف بود، پس پس‌زمینه سفید نگه داشته شد. روی ۱۵۶۳۱ کدکس همه را شفاف کرده. وقتی تأیید شد، این قانون در `storefront/omnio.css` حذف می‌شود تا تینت تم از پشت محصول رد شود:

```css
.omni-quad__art,.omni-six__art,.omni-tile__quad span,.ostore__deptart,.orel__art{ background:#fff; }
```

روش سنجش شفافیت در `docs/background-removal-list.md` هست — نوار یک‌پیکسلی لبه روی بوم ۶۴×۶۴.

---

## معماری

پایه از استورفرانت Marko فورک شده: لایه‌ی داده، اتصال Selldone، سبد، هندآف چک‌اوت و اسکریپت‌های QA مشترک‌اند. **لایه‌ی بصری کاملاً جداست.**

| فایل | نقش |
|---|---|
| `storefront/omnio.css` | کل سیستم بصری Omnio (توکن‌های `--om-*`) |
| `storefront/styles.css` | ارثی از Marko، فقط پایه‌ها |
| `home-omnio.js` → `home-board.js` + `home-hero.js` | صفحه اول |
| `shop-omnio.js` | لیست با فیلتر شمارش‌شده |
| `product-omnio.js` | صفحه محصول سه‌ستونه |
| `cart-omnio.js` · `checkout-omnio.js` · `vendor-omnio.js` | سبد، پرداخت، فروشگاه فروشنده |

**زبان بصری:** کروم تیره‌ی کاربردی، صفحه‌ی خاکستری تا کارت‌های سفید مثل شیء روی قفسه دیده شوند، فاصله‌ی فشرده، دو رنگ کنشی (زرد = افزودن، قرمز = تغییر قیمت). خنثی‌ها عمداً ثابت‌اند؛ تم فقط رنگ‌های کنشی و تینت را می‌گرداند.

---

## تصمیم‌هایی که نباید برگردند

**۱. هیچ فیلد کارتی در چک‌اوت نیست.** جزئیات کارت روی صفحه‌ی رمزنگاری‌شده‌ی ارائه‌دهنده وارد می‌شود. یک استورفرانت نباید فیلدی نشان دهد که شبیه دریافت کارت است.

**۲. تاریخ تحویل ساخته نمی‌شود.** اگر فروشگاه `lead` واقعی دارد نمایش داده می‌شود، وگرنه «در چک‌اوت مشخص می‌شود».

**۳. ریویوها برچسب «sample content» دارند** و در امتیاز حساب نمی‌شوند. به‌محض اینکه محصولی امتیاز واقعی بگیرد، `loadReviews` خودکار جایگزین می‌کند.

**۴. رنگ‌ها هاردکد نمی‌شوند.** تینت باند کمپینی یک توکن تم است؛ رنگ ثابت باعث می‌شود موقع تغییر تم یک مستطیل رنگی جا بماند.

**۵. ردیف‌ها سوراخ نمی‌مانند.** تعداد کارت هر ردیف باید بر تعداد کل بخش‌پذیر باشد، وگرنه یکی تنها می‌ماند. بریک‌پوینت‌ها کارت حذف می‌کنند نه اینکه ردیف را بشکنند.

---

## درس‌های عیب‌یابی از این سشن

**اسکریپت چند بریک‌پوینت را نمونه‌برداری کردن کافی نیست.** سه بار گفتم «تمیز است» در حالی که کاربر برش می‌دید. معیار درست:

```js
[...document.querySelectorAll('*')].filter(e => {
  const cs = getComputedStyle(e);
  if (cs.overflow === 'visible') return false;
  return e.scrollHeight > e.clientHeight + 1 || e.scrollWidth > e.clientWidth + 1;
})
```

هر عنصری که `overflow` غیرقابل‌دید دارد و اسکرول می‌شود، برش می‌خورد. با همین معیار در ۱۶ عرض تست شد.

**`min-height:0` روی فرزندان فلکس.** بدون آن، فرزند زیر ارتفاع محتوایش کوچک نمی‌شود و از باند بیرون می‌زند. علت همان برش «See more» بود.

**اسکرین‌شات زودهنگام دروغ می‌گوید.** چند بار فکر کردم تصویر شکسته است در حالی که `loading="lazy"` بود. باید منتظر `complete && naturalWidth > 0` ماند.

---

## کارهای باز

| کار | وضعیت |
|---|---|
| اتصال به فروشگاه ۱۵۶۳۱ | **اول از همه** |
| برداشتن پس‌زمینه‌ی سفید | بعد از تأیید شفافیت |
| دیپلوی روی omnio.selldone.shop | `wrangler.toml` آماده؛ نیاز به Authorize کلادفلر |
| تطبیق اسکریپت‌های QA | از Marko آمده‌اند و برای طراحی آن تنظیم شده‌اند |
| بسته‌ی نمونه | مثل Marko و Roben، یک ورودی در `build-packages.mjs` |

---

## نکته درباره‌ی کلادفلر

توکن OAuth رنگلر کوتاه‌عمر است و مسیر refresh آن به bot challenge می‌خورد. هر دیپلوی یک `npx wrangler login` و یک کلیک Authorize از کاربر لازم دارد. اجرای `wrangler login` از ابزار عامل روی ویندوز گاهی با کرش libuv می‌میرد؛ در ترمینال خود کاربر پایدارتر است.
